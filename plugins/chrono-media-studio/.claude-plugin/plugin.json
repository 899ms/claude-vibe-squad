{
  "name": "chrono-media-studio",
  "version": "0.1.0",
  "description": "Chrono content generation: images (gpt-image-2, Gemini via the agy CLI, Grok Imagine), videos (Sora 2, Grok Imagine), audio (ElevenLabs). Gemini video/music retired: Gemini runs through agy, which has no video or audio tool. Owner roles: content media specialists (image-designer, video-director, music-composer, sound-designer, voice-narrator, voice-agent-builder).",
  "license": "AGPL-3.0-or-later",
  "mcpServers": {
    "chrono-media-studio": {
      "command": "${CLAUDE_PLUGIN_ROOT}/../../.venv/bin/python",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp_server.py"
      ],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}",
        "XAI_API_KEY": "${XAI_API_KEY}"
      }
    },
    "higgsfield": {
      "type": "http",
      "url": "https://mcp.higgsfield.ai/mcp",
      "auth": "oauth"
    },
    "elevenlabs": {
      "command": "uvx",
      "args": [
        "elevenlabs-mcp==0.12.2"
      ],
      "env": {
        "ELEVENLABS_API_KEY": "${ELEVENLABS_API_KEY}"
      }
    }
  }
}
